{
  "name": "web2md",
  "version": "1.0.0",
  "main": "dist/src/server.js",
  "scripts": {
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node dist/src/server.js",
    "test": "npm run build && node dist/test/smoke-test.js"
  },
  "keywords": [
    "reader",
    "markdown",
    "readability"
  ],
  "author": "",
  "license": "ISC",
  "description": "r.jina.ai style service: prepend the prefix to any URL, get AI-ready Markdown back.",
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "jsdom": "^29.1.1",
    "turndown": "^7.2.4"
  },
  "devDependencies": {
    "@types/jsdom": "^28.0.3",
    "@types/node": "^26.1.2",
    "@types/turndown": "^5.0.6",
    "typescript": "^7.0.2"
  }
}
